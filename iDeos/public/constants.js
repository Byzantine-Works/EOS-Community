const DisplayName = 'iDeos'; 

const Platform = {
  macOS: 'darwin',
  windows: 'win32',
  linux: 'linux'
}

const WindowSize = { width: 900, height: 680 };

module.exports = {
  DisplayName,
  Platform,
  WindowSize
}